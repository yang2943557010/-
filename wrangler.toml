# Cloudflare Pages 静态站点（不要用 npx wrangler deploy）
#
# Dashboard 部署命令请填:
#   npx wrangler pages deploy . --project-name=netdisk-pages
# 或:
#   npm run deploy
#
# 构建命令留空，输出目录为仓库根目录 /
#
# 性能相关（Dashboard 建议开启）:
# - Speed → Optimization → Auto Minify (HTML/CSS/JS)
# - Speed → Optimization → Brotli
# - Caching → 遵循仓库根目录 _headers 的 Cache-Control
#
# API Worker（微信代理/短链）单独部署:
# npx wrangler deploy cloudflare-worker.js

name = "netdisk-pages"
compatibility_date = "2024-08-01"
pages_build_output_dir = "."

# 纯静态站点，无需 build 命令
